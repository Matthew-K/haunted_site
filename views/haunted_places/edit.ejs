<% include ../partials/header.ejs %>

<div class="container form"> 
	<form action="/haunted_places/<%= haunted_place._id %>?_method=PUT" method="post">
		<div class="form-group">
			<label>Name</label>
		    <input type="text" name="haunted_place[name]" value="<%= haunted_place.name %>" class="form-control">
		</div>
		<div class="form-group">
			<label>Image</label>
		    <input type="text" name="haunted_place[image]" value="<%= haunted_place.image %>" class="form-control">
		</div>
		<div class="form-group">
			<label>Description</label>
			<textarea class="form-control" rows="5" name="haunted_place[description]"><%= haunted_place.description %></textarea>
		</div>
		<button type="submit" class="btn btn-default">Update</button>
	</form>
</div>

<% include ../partials/footer.ejs %>